# 개요

- 이 프로젝트는 웹소설 생성기의 Front end 를 구성한다.

# 실행

- 개발:

```bash
npm --workspace ./packages/frontend install
npm --workspace ./packages/frontend run dev
```

- 앱은 기본적으로 `http://localhost:3000`에서 실행됩니다.
